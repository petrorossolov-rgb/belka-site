# Общие map для server-блоков belkascm (T15). Файл из sites-enabled попадает
# в контекст http, где только и можно объявить map.
map $uri $belkascm_cache_control {
    ~^/_astro/  "public, max-age=31536000, immutable";
    default     "no-cache";
}
