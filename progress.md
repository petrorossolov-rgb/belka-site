# Execution Progress

## Codebase Patterns
<!-- Reusable patterns discovered during implementation. Keep this section at the top. -->

- **Smart App Control на рабочей машине владельца блокирует нативный `satteri`** (Markdown-процессор Astro 7, `@astrojs/markdown-satteri`): `An Application Control policy has blocked this file … satteri_napi.win32-x64-msvc.node`. Остальные нативные модули (rolldown, sharp, lightningcss, компилятор Astro) грузятся. Обход — только локальный: после каждого `npm ci` / `npm install` (включая `npm install -D <пакет>`: он тоже удаляет обход, ep01 T04) выполнить `npm install --no-save --force @bruits/satteri-wasm32-wasi@<версия satteri из npm ls satteri>`. Загрузчик satteri сам падает на WASI-сборку, если нативная не загрузилась. `package.json` и `package-lock.json` не меняются, в CI (Linux) грузится нативная сборка. Симптом без обхода: `astro check` / `vitest` / `astro build` падают с `Cannot find native binding`. (ep01 T02)
- **В Windows-окружении нет `python`** — скриптовые правки файлов делаются через `node`. (ep01 T01)
- **`tsconfig.json` исключает `docs`**: иначе `astro check` проверяет JS дизайн-источников в `docs/inputs/` и шумит подсказками. (ep01 T02)
- **Две ступени токенов**: примитивы ДС — `src/styles/tokens/*.css` (копия без изменений, тест `contrast.test.ts` сверяет со снимком `tests/fixtures/ds-sources.json`, снимок с `docs/inputs/…` — `ds-sources.test.ts`); семантика — только `src/styles/theme.css`. Имена семантики не повторяют имена примитивов (`--radius-sm` занят ДС → `--radius-field`; `--space-1…9` → `--space-2xs…2xl`), иначе `var()` зацикливается. Компоненты используют только семантику. (ep01 T03)
- **`sharp()` не принимает `URL`**: в скриптах путь из `new URL(…, import.meta.url)` передаётся через `fileURLToPath`. (ep01 T05)
- **`Logo.astro` — единственная точка вывода знака**: `size` — высота в CSS px, предел — половина исходника (`src/lib/logo.ts`); обязателен `alt` либо `decorative`. Фавиконки пересобираются `npm run build:favicons` и коммитятся. (ep01 T05)
- **Id записи контента = путь файла без `.md`**: коллекции на `glob` в `src/content.config.ts` задают `generateId` по пути. Стандартный slugify понижает регистр и учитывает `slug` из frontmatter и прячет нарушения формата id от `assertContentIntegrity`. Новые коллекции — через тот же хелпер `markdown(base)`. (ep01 T06)
- **Схемы с `image()` — фабрики** в `src/lib/schemas.ts` (`pageSchema(image)`, `siteSchema(image)`); в тестах `image` подменяется на `() => z.string()`, `reference()` — на `z.string()`. `astro:*` в `schemas.ts` не импортируется. (ep01 T06)
- **Видимая высота знака — токен, `size` у `Logo` — разрешение растра.** Предок задаёт `--logo-height: var(--logo-…-h)` (токены в `theme.css`), `Logo.astro` берёт её, иначе высоту растра (`--logo-size`). `size` должен быть не меньше максимума токена, иначе знак растягивается. Так lockup заглушки ужимается до 360px без горизонтального скролла. (ep01 T08)
- **Страницы — через `Base.astro`**: `title` (готовая строка), `description`, `draft` (маркер `<html data-draft>` в любом окружении + плашка там, где черновики видны), `brandScreen` (страница сама показывает знак и слоган крупно: шапка без знака, подвал без имени и слогана). Раскладка — `.container`, надзаголовок — `.eyebrow` из `global.css`. (ep01 T08)
- **Скрипты-гейты в `scripts/*.mjs`** — `// @ts-check` + JSDoc (их проверяет `astro check`, отдельный `tsc` не нужен); логика — экспортируемая чистая функция, CLI — обёртка под `if (process.argv[1] && resolve(process.argv[1]) === fileURLToPath(import.meta.url))`, код 1 — нарушения, 2 — неверный вызов. Тесты импортируют функцию и правят копию фикстуры во временном каталоге (`mkdtempSync` + `cpSync`), CLI проверяют через `spawnSync(process.execPath, …)`. Пример — `check-dist-seo.mjs`. (ep01 T16)
- **Правила окружения сборки — в `src/lib/site-env.ts`** (`indexable`, `robotsTxt`, `sitemapEnabled`, `sitemapPageFilter`): компоненты и эндпоинты берут `SITE_ENV` из `astro:env/server` и не сравнивают его со строками сами. (ep01 T16)
- **Stylelint (`npm run lint:css`)**: литералы цвета, `rgb()`/`hsl()`/`color-mix()` и `px|rem|em|ms|s` в размерах, отступах, радиусах, движении — только в `src/styles/tokens/**` и `theme.css`; в компонентах — `var(--…)`, `0`, `%`, `fr`, `vw`, `vh`, безразмерный `line-height`, литералы в условиях `@media`. Охват — любые размеры (`width`/`height` с `min-`/`max-`, `*-size`, `flex`, `grid-template-*`), шорткаты `border`/`outline`/`font`, `transform`/`translate` и кастомные свойства `--…` вне слоя темы; фиксирует `tests/stylelint-tokens.test.ts` (/my-verify ep01, F03 Codex). Классы — kebab-case с BEM (`block__element--modifier`), `@import` — строкой, hex — шестью знаками. Примитивы в `tokens/**` не переформатируются (дословная копия ДС). (ep01 T22)
- **Симлинки в Git Bash**: `ln -s` по умолчанию копирует, нативные симлинки без режима разработчика запрещены. Shell-тесты с симлинками под `MINGW*|MSYS*` выставляют `MSYS=winsymlinks:sys` (эмуляция MSYS с настоящей семантикой `-L`/`readlink`/`mv -T`); на Linux не нужно. Пример — `infra/deploy/tests/belka-deploy.test.sh`. (ep01 T11)
- **Shell-скрипты в `infra/`**: `set -euo pipefail`, `LC_ALL=C`, `shellcheck` без замечаний, режим `100755` в git (`git update-index --chmod=+x`), LF (`.gitattributes`). shellcheck на машине владельца — из winget (`koalaman.shellcheck`); если оболочка открыта до установки, он лежит в `%LOCALAPPDATA%\Microsoft\WinGet\Packages\koalaman.shellcheck_…\shellcheck.exe`. (ep01 T11)
- **Два репозитория в одной рабочей папке** (с 2026-09-24): корень — публичный код `belka-site`, `docs/` — отдельный git приватного `belka-site-docs` (корень игнорирует `docs/`). Задача с правками кода и `tasks.md`/`log.md` — по коммиту в каждом, сообщение одно и то же. Код не читает `docs/` в тестах и сборке: исходники ДС — снимок `tests/fixtures/ds-sources.json` (значения примитивов и SHA-256 растров), его сверку с `docs/inputs` делает `tests/ds-sources.test.ts` при наличии `docs/`. Обновление ДС — скопировать файлы в `src/`, пересобрать снимок, `npm run check:docs`. Черновики текстов — в `docs/`, в `src/content/` только утверждённое. (ep01, разделение перед сессией F)
- **`npm run check:docs`** — локальный гейт документов: lychee офлайн по `docs/**/*.md`, `CLAUDE.md`, `progress.md` и сверка снимка ДС с `REQUIRE_DOCS=1`. Запускается pre-commit-хуком `docs/.git/hooks/pre-commit`; хук не версионируется, после клонирования `belka-site-docs` его ставят заново. lychee — из winget (`lycheeverse.lychee`). (ep01)
- **Итог гейтов — задание `gates-ok` в `gates.yml`** (required check `gates / gates-ok`, `app_id` GitHub Actions): `needs` на все задания гейтов, `if: always()` и явное падение, если хоть один результат не `success`. Задание, пропущенное из-за упавшего `needs`, GitHub засчитывает required check как успех. Новый гейт добавляется в `needs`, имя проверки не меняется. У шагов гейтов условий, `continue-on-error` и `|| true` нет. (ep01 T10)
- **Push в GitHub — через `gh`, а не Git Credential Manager**: в `.git/config` обоих репозиториев `credential.https://github.com.helper` сброшен и равен `!gh auth git-credential` (`username = petrorossolov-rgb`). Перед push — `gh auth status`: активен `petrorossolov-rgb`. `main` защищён, изменения — только веткой и PR. (ep01 T10)
- **Merge PR на стороне GitHub подписывается почтой аккаунта** — при любом способе: merge-коммит и squash ставят её в автора, rebase — в коммитера. Локальный noreply-адрес на это не влияет. Поэтому у `petrorossolov-rgb` включено «Keep my email addresses private» (Settings → Emails; через API не задаётся, нужен скоуп `user`). С ним веб-операции подписываются noreply-адресом. PR мёржится `gh pr merge <N> --rebase`: в истории остаются коммиты задач без merge-коммитов. После каждого merge — `git log -3 --format='%h %ae %ce' origin/main`: только noreply-адреса. Если там появилась настоящая почта, значит, настройка слетела: остановиться и сообщить владельцу. (ep01 T23, исправлено в сессии F)
- **Lighthouse CI**: `lighthouserc.{mobile,desktop}.cjs` строятся из `lhciConfig()` в `lighthouserc.base.cjs`; страницы — из sitemap прод-сборки плюс `/404.html` (`scripts/lhci-urls.mjs`), ручного списка нет. Запуск — `npm run lhci:mobile|lhci:desktop` на `dist/` прод-сборки. Локально на Windows `chrome-launcher` падает на уборке профиля (`EPERM`), поэтому отчёты смотрят в artifact `.lighthouseci` прогона CI. (ep01 T23)
- **lychee 0.24**: `include_fragments` — строка (`"anchor-only"`), не `true`. `--root-dir` — абсолютный путь. В CI проверяется `dist/` (`lychee.toml`), документы — `npm run check:docs`. (ep01 T23)
- **nginx belkascm: все `add_header` — на уровне `server`**, в наших `location` своих нет: `add_header` в `location` отменяет унаследованные заголовки безопасности. Различия по пути — через `map` в контексте `http` (`infra/nginx/belkascm-maps.conf`, пример — `$belkascm_cache_control`), заголовок — `add_header … $переменная always` в сниппете. Сниппеты — `/etc/nginx/snippets/belkascm/`. HTTPS-версии конфигов ссылаются на `/etc/letsencrypt/live/<имя>/`, поэтому сертификат выпускается (`certbot certonly --webroot`, сначала `--dry-run`) до их установки. (ep01 T15)
- **Проверки с секретами не печатают команду**: `echo "### $*"` + `curl -u …` выводит пароль в журнал сессии. В таких проверках печатать только код ответа и заголовки. (ep01 T15)
- **Скрипты-проверки для workflow — в `infra/<назначение>/*.sh`**, а не шагами YAML: их прогоняют локально (Git Bash) и проверяет shellcheck в `gates.yml` (новый скрипт — в список шага «shellcheck скриптов infra»). Пороги — переменные окружения с дефолтом (`MIN_DAYS=400 bash infra/monitor/check-site.sh`), код 1 — проверка не прошла, 2 — неверный вызов. (ep01 T19)
- **Доставка — `deploy.yml` + `rollback.yml`, клиент сервера — `infra/deploy/ci-deploy.sh`, смоук — `infra/deploy/smoke.sh`.** Выкладывается artifact из прогона `gates` того же workflow; выкладка и смоук — шаги одного job с `environment:` (`concurrency: deploy-<env>` у обоих workflow). Промоушн вызывает `gates` с `site_env: staging` и `ref: <sha>` — те же гейты, что у стейджинга, плюс staging-`draft-routes.json` для проверки 404 черновиков на проде. Проверка «SHA на стейджинге» — успешный deployment `staging` (статус `success` ищется в истории: после следующей выкладки он становится `inactive`) **и** успешный push-прогон `deploy.yml`: `rollback.yml` тоже создаёт deployment, но с SHA запуска. Reusable `gates.yml` и `infra/deploy` в прогоне промоушна берутся из HEAD `main`, а не из SHA, поэтому SHA ≠ HEAD промоутится, только если между ними не менялись `.github/` и `infra/deploy/` (`infra/deploy/promote-guard.sh` по compare API, /my-verify ep01, F01 Codex). Смоук знает только инварианты окружения; с `--dist` тело `/` и 404 сверяется побайтно со сборкой. (ep01 T17, T18)
- **actionlint — через Docker**: `MSYS_NO_PATHCONV=1 docker run --rm -v "$(pwd -W):/repo" -w /repo rhysd/actionlint:latest -no-color` (в образе есть shellcheck для `run:`). В YAML `run: echo "…: …"` без блочного скаляра не парсится — двоеточие с пробелом; такие строки — через `run: |`. (ep01 T17)
- **Автоматический режим Claude не выполняет без явного «да» владельца в этой же сессии** `gh workflow run deploy.yml|rollback.yml` (выкладка в окружения) и, как правило, `gh pr merge` («Merge Without Review»; в сессии I первый merge прошёл, второй — нет). Эти шаги собираются в один список и подтверждаются владельцем заранее. (ep01 T17)
- **Пограничная проба гейта**: у каждого гейта есть проба не только на грубую поломку, но и на границу заявленного: агрегация (медиана, а не лучший прогон), охват правила (все свойства, которые обещает принцип), состояние, на котором гейт должен отказать (промоушн SHA ≠ HEAD). Проба — регресс-тест, который падает на мутанте с прежним поведением. Негативы T24 доказали только грубые случаи, а медиану Lighthouse, охват Stylelint и `promote-guard` нашёл `/my-verify`. Новый гейт (линтер голоса в ep02) приходит вместе с такой пробой. Тест гейта не читает `dist/` и другие артефакты сборки: в `gates.yml` тесты идут до сборки, берётся фикстура (`tests/fixtures/dist-production/`). (/my-verify ep01)

- **Контракты каркаса для ep02+** (ep01 T25):
  - **«Продукт — это файл»**: `getCollection`/`getEntry` вызываются только в `src/lib/content.ts`; страницы и компоненты берут данные через его функции (`getVisible`, `getNav`, `getPlatformMap`, `getProductPages`, …). Новый продукт — файл `src/content/products/{id}.md` по схеме; навигация, карта и sitemap строятся из коллекций, ручных списков нет. Проверка — `grep -rnE "getCollection|getEntry" src` находит только `content.ts`.
  - **Токены**: литералы цвета, размеров, отступов, радиусов и движения — только в `src/styles/tokens/**` и `src/styles/theme.css`; механически держит Stylelint (см. выше «Две ступени токенов» и «Stylelint»).
  - **`SITE_ENV`-модель черновиков**: `production` исключает `draft: true` отовсюду (маршруты, навигация, sitemap); `staging` и `development` показывают черновик с плашкой и маркером `<html data-draft>`, всё окружение — `noindex` и `Disallow: /`. `check-dist-seo` пишет маршруты черновиков в `draft-routes.json`, смоук прода проверяет, что они отдают 404. Правила — только в `src/lib/site-env.ts` и `content-core.ts`.
  - **Гейты — только в `.github/workflows/gates.yml`**: `ci.yml`, `deploy.yml` вызывают его, своих проверок не имеют. Новый гейт — шаг в `gates.yml` и, если это отдельное задание, пункт `needs` у `gates-ok`; без `continue-on-error`, условий и `|| true`.
- **SEO-слой — `src/lib/seo.ts` + `Seo.astro`**: страница передаёт в `Base` готовый `title` через `buildTitle(title, site.name, { home })` (у продукта — `productTitle`), `ogImage` из записи. Canonical и `og:url` — `canonicalUrl(site.url, Astro.url.pathname)` от `site.yaml → url` в любом окружении (на стейджинге тоже прод-адрес). Страница без своего адреса (404) передаёт `canonical={false}`: без canonical и `og:url`; `noindex` ей не ставить — LHCI меряет SEO на `/404.html`. JSON-LD — только `Organization` из `site.yaml`, без персон (тест `seo.test.ts`); сериализация — `serializeJsonLd` (экранирует `<`, `>`, `&`). (ep01 T21)
- **Инструмент записи файлов Claude превращает escape-последовательности U+2028/U+2029 (обратная косая черта, «u2028») в сырые символы-разделители строк** — TS/JS перестаёт разбираться, Markdown ломается. Такие символы в коде — через `String.fromCharCode(0x2028)`; экранированную форму в строке-результате вписывать скриптом с `String.fromCharCode(92)` и проверять `cat -v` (пример — `serializeJsonLd` в `src/lib/seo.ts`). (ep01 T21)

## Docs Debt
<!-- Items logged by /my-execute, /my-change, /my-incident. Resolved by /my-sync-docs. -->

- [ ] 2026-09-25 Update `docs/ep01-foundation/plan.md` → «Контракт CI» и B8 — у `gates.yml` вход `ref` (SHA промоушна); промоушн вызывает `gates` с `site_env: staging` (не `production`), выкладывает `dist-production`; проверка промоушна — deployment `staging` + успешный push-прогон `deploy.yml`; скрипты `infra/deploy/{ci-deploy,smoke}.sh`. (ep01 T17)

- [ ] 2026-09-25 Update `docs/ep01-foundation/plan.md` → B6 и бриф T15 в `tasks.md` — `Permissions-Policy` без `interest-cohort` (FLoC снят, Chrome пишет ошибку в консоль — аудит Lighthouse); заголовки ставятся на уровне `server` с `map` для `Cache-Control`, а не сниппетом в каждой `location`. (ep01 T15)
- [ ] 2026-09-25 Update `docs/ep01-foundation/plan.md` → C1 — у 404 нет canonical и `og:url` (`canonical={false}`), `contactPoint` в JSON-LD без `contactType`; C7 и бриф T24 в `tasks.md` — невалидный по схеме контент падает на `astro check`, а шаг сборки ловит нарушения целостности набора (`assertContentIntegrity`). (ep01 T21, T24, резюме сессии J)
- [ ] 2026-09-25 Update дрейф из /my-verify ep01: `infra/README.md:142` — `www.belkascm.ru` отвечает 301, а не 200; `docs/runbooks/deploy.md:83` — описание строки смоука `ETag; Last-Modified` сверить с фактом; `docs/ep01-foundation/session-plan.md` — в карте сессий у F «Приватный репозиторий», фактически код публичный, документы приватные; `plan.md` — имя портала. (/my-verify ep01)
- [ ] 2026-09-24 Update `docs/ep01-foundation/plan.md` → «Контракт деплоя» — у `belka-deploy` сверх кодов 0/2/64 есть 65 (негодный архив: пустой, не tar.gz, без `index.html`, `..`, абсолютные пути, симлинки и жёсткие ссылки — запрещены все) и 1 (прочие сбои). Хранение — 5 последних по времени выкладки плюс текущий. В runbook `docs/runbooks/deploy.md` учтено (T18), в `plan.md` — нет. (ep01 T11)

## Follow-ups
<!-- Tasks deferred from /my-incident or /my-change that need proper implementation later. -->

- [ ] 2026-09-25 TTL зоны belkascm.ru в reg.ru — 86400 (B2 предполагал 300 на время запуска). Перед любой правкой зоны снизить TTL до 300 и дождаться истечения старого; после правки и недели стабильности вернуть рабочее значение (3600 и выше). Решение по окончательному значению — владельца. (ep01 T14, T25)
- [ ] 2026-09-25 HSTS: поднять `Strict-Transport-Security` с `max-age=300` до `max-age=31536000` после недели стабильности HTTPS (не раньше 2026-10-02) — правка `infra/nginx/snippets/security-headers.conf` через PR, установка на сервер по runbook. `includeSubDomains` и `preload` — только отдельным решением: они задевают `staging` и любые будущие поддомены. Смоук значение `max-age` не сверяет. (ep01 T25)
- [ ] 2026-09-25 `monitor.yml`: GitHub отключает scheduled workflows через 60 дней без коммитов в репозитории. Если между эпиками пауза дольше ~50 дней — пустой коммит или `gh workflow enable monitor.yml`; статус — `gh workflow list`. Порядок — `docs/runbooks/deploy.md`. (ep01 T19, T25)
- [ ] 2026-09-25 Сертификат соседнего сайта на VPS не продлевается: его A/AAAA указывают на хостинг reg.ru, `certbot renew --dry-run` по нему — failure (404 от чужого сервера). Истекает 2026-11-13. Решение владельца: вернуть DNS соседа на VPS или убрать его сертификат и vhost. Наши сертификаты от этого не зависят. (ep01 T15)

- [ ] 2026-09-25 С 2026-10-19 `ubuntu-latest` — Ubuntu 26: гейты могут упасть (Chrome, shellcheck, tar). Падение — дефект, чинить через `/my-change`, не отключать и не закреплять старый образ без решения (Constitution 8). (ep01 T17)
- [ ] 2026-09-25 Кандидаты на `/my-change` из /my-verify ep01: пининг `actions/*` по SHA + Dependabot для actions (F02 Codex, принятый долг T10); тест равенства адреса прода в `site.yaml`, `src/lib/site-env.ts` и `scripts/check-dist-seo.mjs`; гейт «`getCollection`/`getEntry` только в `src/lib/content.ts`» тестом вместо `grep`; проверка коллизий маршрутов `page` ↔ `products/[id]` / `404`. (/my-verify ep01)
- [ ] 2026-09-25 ep05: Метрика упрётся в собственные гейты — `check-dist-seo` запрещает любой `<script>`, кроме JSON-LD, CSP разрешает только `'self'`. Проектировать в research ep05 как осознанное расширение гейта под флагом юрлица, а не ослабление (Constitution 4, 8). (/my-verify ep01)
- [ ] 2026-09-24 Когда владелец переедет на свой CI или хостинг git, вернуть код и документы в один приватный контур: lychee по `docs/` — снова шаг CI, снимок ДС можно оставить. Архив прежней истории — `C:\Proj\WMS\belka-site-history.git`. (ep01, разделение репозитория)

- [ ] 2026-09-24 Если обновление Astro поменяет версию `satteri`, локальный WASI-обход нужно ставить с новой версией (см. Codebase Patterns). Лучше, чтобы нативный модуль получил подпись или репутацию и обход стал не нужен. Проверять при каждом `/my-change` мажоров. (ep01 T02)
- [ ] 2026-09-24 `Logo.astro` импортирует три оттенка словесного знака, и Astro кладёт в `dist/_astro/` оригиналы неиспользуемых (`wordmark-latin-milk|rust.png`, ~117 КБ без ссылок). Безвредно, но если T16 (`check-dist-seo`) или lychee начнут проверять «лишние» файлы, либо понадобится чистый `dist` — перейти на выбор оттенка без статического импорта всех трёх (например, отдельные компоненты или карта `import.meta.glob` с `eager: false` и проверкой, что Vite не выпускает остальное). (ep01 T05)
